program simples3;
const
    a = 2;
var
    b = 5;
begin
    (*c := 2;*) (*nao consegue criar mais*)
    (*readln(a);*) (*o read nao altera constante, verifica depois de pegar no teclado*)
    (*readln(d);*) (*o read nem cria variavel*)
    writeln(b);
    readln(b);
    writeln('O valor da variavel agr eh:',b) (*nao tem ; antes de end*)
end.